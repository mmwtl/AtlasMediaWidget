android {
    signingConfigs {
        release {
            storeFile rootProject.file("atlasmediawidget-release.jks")
            storePassword ""
            keyAlias ""
            keyPassword ""
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
        }
    }
}
